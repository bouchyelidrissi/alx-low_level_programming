#include "main.h"

/**
 * _isdigit - checks if c digit.
 * @c: input.
 * Return: Returns 1 if c is digit , Returns 0 otherwise.
 */

int _isdigit(int c)
{

if (c >= 48 && c <= 57)
{
return (1);

}
else
{
return (0);
}

}
